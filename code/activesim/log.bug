
      model {
      for (i in 1:N) {
      y[i] ~ dbern(p[i])
      logit(p[i]) <- b1*Var1[i]+ b2*Var2[i]+ b3*Var3[i]+ b4*Var4[i]
      }
      b1 ~ dnorm(0.0, 0.001)
      b2 ~ dnorm(0.0, 0.001)
      b3 ~ dnorm(0.0, 0.001)
      b4 ~ dnorm(0.0, 0.001)
      }
      
